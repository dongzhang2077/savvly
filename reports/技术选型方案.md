# Savvly 技术选型方案

## 项目约束与目标

### 关键约束条件

```
✓ 学期作业项目（有明确考核要求和时间限制）
✓ Web 优先（满足学期考核需求）
✓ 未来移动端扩展（如果项目成功继续开发）
✓ 时间有限（一个学期内完成 MVP）
✓ 需要演示展示（给教授/同学/潜在用户）
```

### 项目目标

- **短期**：完成学期作业，获得高分，构建可用 MVP
- **中期**：吸引早期用户，验证产品市场契合度
- **长期**：发展成真实产品，扩展到移动端

---

## 最终选择：方案 A - 现代全栈方案 ⭐

### 核心技术栈

```
前端框架：
├─ Next.js 14 (App Router)
├─ React 18+
├─ TypeScript
└─ Tailwind CSS + Shadcn/ui

后端：
├─ Next.js API Routes (一体化架构)
├─ Prisma ORM
└─ PostgreSQL

认证：
├─ NextAuth.js v5
└─ JWT + Session

AI/智能分类：
├─ 规则引擎（MVP 阶段）
└─ OpenAI API（后续升级）

部署：
├─ Vercel (前端 + API)
└─ Railway (PostgreSQL)

开发工具：
├─ GitHub (版本控制)
├─ ESLint + Prettier (代码规范)
└─ Vercel Analytics (用户行为分析)
```

---

## 技术选型理由

### 1. Next.js 14 - 一体化全栈框架

**为什么选择 Next.js？**

#### 一体化开发体验

```
单一项目包含：
├─ 前端 React 组件（/app/page.tsx）
├─ API 路由（/app/api/*/route.ts）
├─ Server Components（服务端渲染）
└─ Client Components（客户端交互）

优势：
✓ 减少前后端分离的复杂性
✓ 共享 TypeScript 类型定义
✓ 统一的代码风格和工具链
✓ 简化部署流程（一键部署）
```

#### 性能优化

```
内置优化：
├─ 自动代码分割（Code Splitting）
├─ 图片优化（Next/Image）
├─ Server Side Rendering（首屏快速）
├─ Static Site Generation（静态页面）
└─ 预加载（Prefetch）

结果：
✓ 更快的加载速度
✓ 更好的 SEO（如果需要）
✓ 更低的服务器成本
```

#### 开发体验

```
App Router 优势：
├─ 文件系统路由（约定优于配置）
├─ 布局系统（嵌套布局）
├─ 加载状态（loading.tsx）
├─ 错误处理（error.tsx）
└─ Server Actions（服务端操作）

例子：
app/
├─ (dashboard)/
│  ├─ dashboard/page.tsx → /dashboard
│  ├─ budgets/page.tsx   → /budgets
│  └─ layout.tsx         → 共享布局
└─ api/
   └─ transactions/route.ts → /api/transactions
```

#### 部署无脑

```
Vercel 集成：
├─ git push → 自动部署
├─ 预览 URL（每个 PR 独立预览）
├─ 免费 HTTPS + CDN
├─ 环境变量管理
└─ 零配置生产环境

开发流程：
1. git commit -m "新功能"
2. git push
3. ✓ 自动部署完成
4. 访问 https://savvly.vercel.app
```

---

### 2. TypeScript - 类型安全

**为什么必须用 TypeScript？**

#### 类型安全带来的好处

```typescript
// 编译时错误检查
interface Transaction {
  id: string;
  amount: number;
  category: string;
  date: Date;
}

// 错误会在编写时发现，而非运行时
function createTransaction(data: Transaction) {
  return data.amount.toFixed(2); // ✓ 类型正确
  // return data.amount.toUpperCase(); // ✗ 编译错误
}

优势：
✓ 提前发现 90% 的 bug
✓ 重构更安全（IDE 自动提示影响范围）
✓ 团队协作更清晰（接口即文档）
✓ IntelliSense 自动补全
```

#### 与 Prisma 的完美配合

```typescript
// Prisma 自动生成类型
import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

// 自动类型推导
const user = await prisma.user.findUnique({
  where: { email: "dong@example.com" },
  include: { budgets: true }, // ← IDE 自动提示可用关系
});

// user 的类型自动推导为：
// User & { budgets: Budget[] }

// 访问不存在的字段会编译错误
console.log(user.name); // ✓ 正确
console.log(user.fake); // ✗ 编译错误
```

#### 适合复杂业务逻辑

```typescript
// Savvly 的预算回滚逻辑很复杂
type RolloverStrategy = 'full' | 'partial' | 'none' | 'goal';

interface Budget {
  id: string;
  category: string;
  amount: number;
  rolloverType: RolloverStrategy;
  rolloverAmount: number;
}

function calculateRollover(
  budget: Budget,
  actualSpent: number
): number {
  const remaining = budget.amount - actualSpent;

  switch (budget.rolloverType) {
    case 'full':
      return remaining > 0 ? remaining : 0;
    case 'partial':
      return remaining > 0 ? remaining * 0.5 : 0;
    case 'none':
      return 0;
    case 'goal':
      return budget.amount + remaining; // 持续累积
    default:
      // TypeScript 确保所有情况都处理
      const _exhaustive: never = budget.rolloverType;
      return 0;
  }
}

优势：
✓ 复杂逻辑不易出错
✓ 边缘情况强制处理
✓ 代码自文档化
```

---

### 3. Prisma ORM - 现代数据库工具

**为什么选择 Prisma？**

#### 开发体验极佳

```prisma
// prisma/schema.prisma - 声明式定义

model User {
  id       String   @id @default(cuid())
  email    String   @unique
  budgets  Budget[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Budget {
  id       String @id @default(cuid())
  userId   String
  user     User   @relation(fields: [userId], references: [id])
  category String
  amount   Float

  @@unique([userId, category])
}

优势：
✓ 清晰易读（类似 Rails）
✓ 关系定义直观
✓ 自动生成 SQL Migration
```

#### 类型安全的查询

```typescript
// 自动生成的类型安全客户端
import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

// 创建用户和预算（关联创建）
const user = await prisma.user.create({
  data: {
    email: 'dong@example.com',
    budgets: {
      create: [
        { category: '食品', amount: 400 },
        { category: '交通', amount: 200 }
      ]
    }
  },
  include: { budgets: true } // 自动 JOIN
});

// 类型自动推导为：
// User & { budgets: Budget[] }

// 复杂查询同样类型安全
const budgets = await prisma.budget.findMany({
  where: {
    userId: user.id,
    amount: { gte: 300 } // 大于等于 300
  },
  orderBy: { amount: 'desc' }
});

优势：
✓ 无需手写 SQL（但支持原始 SQL）
✓ 自动防止 SQL 注入
✓ 关系查询优雅（N+1 问题自动优化）
```

#### Prisma Studio - 可视化管理

```bash
npx prisma studio

# 打开浏览器 http://localhost:5555
# 可视化查看和编辑数据库
# 类似 phpMyAdmin，但更现代

优势：
✓ 开发期间快速查看数据
✓ 手动修复数据问题
✓ 理解数据关系
```

#### Migration 管理

```bash
# 创建新 Migration
npx prisma migrate dev --name add_rollover_to_budget

# Prisma 自动：
# 1. 对比 schema.prisma 和数据库差异
# 2. 生成 SQL Migration 文件
# 3. 执行 Migration
# 4. 重新生成 TypeScript 类型

# 生产环境部署
npx prisma migrate deploy

优势：
✓ Migration 历史清晰（Git 版本控制）
✓ 回滚简单（Prisma 自动处理）
✓ 团队协作无冲突
```

---

### 4. Tailwind CSS + Shadcn/ui - 快速 UI 开发

**为什么选择这个组合？**

#### Tailwind CSS - Utility-First

```tsx
// 传统 CSS
<div className="card">
  <h2 className="card-title">可用收入</h2>
  <p className="card-amount">$1,800</p>
</div>

// Tailwind CSS
<div className="flex items-center justify-between p-4 bg-blue-50 rounded-lg">
  <span className="text-lg font-semibold text-gray-700">可用收入</span>
  <span className="text-2xl font-bold text-green-600">$1,800</span>
</div>

优势：
✓ 不需要写 CSS 文件
✓ 样式与组件在同一处（局部性）
✓ 响应式设计简单（sm: md: lg: xl:）
✓ 暗色模式内置支持（dark:）
✓ Purge CSS 自动移除未用样式（小体积）
```

#### Shadcn/ui - 可复制组件

```bash
# 不是 npm 包，而是复制组件代码到项目
npx shadcn-ui@latest add button
npx shadcn-ui@latest add card
npx shadcn-ui@latest add dialog

# 组件代码直接添加到 components/ui/
# 完全可自定义，不是黑盒

优势：
✓ 美观的组件（现代设计）
✓ 完全控制（可修改源码）
✓ 无运行时依赖（只是代码模板）
✓ Tailwind 原生集成
✓ Accessibility 内置（无障碍）
```

#### 快速开发示例

```tsx
// 安装组件
// npx shadcn-ui@latest add card

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function BudgetCard({ budget }: { budget: Budget }) {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="flex items-center gap-2">
          <span className="text-2xl">{budget.icon}</span>
          {budget.category}
        </CardTitle>
      </CardHeader>
      <CardContent>
        <div className="space-y-2">
          <div className="flex justify-between text-sm">
            <span className="text-gray-600">已用</span>
            <span className="font-semibold">${budget.spent}</span>
          </div>
          <div className="w-full bg-gray-200 rounded-full h-2">
            <div
              className="bg-green-500 h-2 rounded-full transition-all"
              style={{ width: `${(budget.spent / budget.amount) * 100}%` }}
            />
          </div>
          <div className="flex justify-between text-sm">
            <span className="text-gray-600">预算</span>
            <span className="font-semibold">${budget.amount}</span>
          </div>
        </div>
      </CardContent>
    </Card>
  );
}

开发时间：~15 分钟（vs 传统方式 2 小时）
```

---

### 5. PostgreSQL - 生产级数据库

**为什么不用 MongoDB/MySQL？**

#### PostgreSQL 的优势

```
关系型数据库的优势：
├─ 数据一致性（ACID 事务）
├─ 复杂关系查询（JOIN）
├─ 数据完整性约束（外键、唯一性）
└─ 成熟稳定（30+ 年历史）

Savvly 的数据模型：
User ─┬─ Budget ─── Transaction
      ├─ Goal
      └─ Settings

这是典型的关系型数据
→ PostgreSQL 最合适

vs MongoDB（文档型）：
✗ 关系查询复杂
✗ 数据冗余
✗ 事务支持弱

vs MySQL：
⚠️ JSON 支持较弱
⚠️ 全文搜索较弱
✓ PostgreSQL 更现代
```

#### PostgreSQL 特色功能

```sql
-- JSON 支持（存储复杂配置）
CREATE TABLE user_settings (
  id UUID PRIMARY KEY,
  preferences JSONB, -- 可查询的 JSON
  ...
);

-- 查询 JSON 字段
SELECT * FROM user_settings
WHERE preferences->>'theme' = 'dark';

-- 全文搜索（交易描述搜索）
SELECT * FROM transactions
WHERE to_tsvector('english', description) @@ to_tsquery('grocery');

-- 数组类型（标签）
CREATE TABLE transactions (
  id UUID PRIMARY KEY,
  tags TEXT[], -- 数组类型
  ...
);

优势：
✓ 功能最强大的开源数据库
✓ Prisma 完美支持
✓ Railway/Vercel Postgres 托管方便
```

---

### 6. NextAuth.js v5 - 认证解决方案

**为什么选择 NextAuth？**

#### 与 Next.js 完美集成

```typescript
// app/api/auth/[...nextauth]/route.ts
import NextAuth from "next-auth";
import CredentialsProvider from "next-auth/providers/credentials";

const handler = NextAuth({
  providers: [
    // 邮箱密码登录
    CredentialsProvider({
      name: "Credentials",
      credentials: {
        email: { label: "Email", type: "email" },
        password: { label: "Password", type: "password" },
      },
      async authorize(credentials) {
        // 验证用户
        const user = await prisma.user.findUnique({
          where: { email: credentials.email },
        });

        if (
          user &&
          (await bcrypt.compare(credentials.password, user.password))
        ) {
          return user;
        }
        return null;
      },
    }),
  ],
  callbacks: {
    async jwt({ token, user }) {
      if (user) {
        token.id = user.id;
      }
      return token;
    },
    async session({ session, token }) {
      session.user.id = token.id;
      return session;
    },
  },
});

export { handler as GET, handler as POST };
```

#### 保护路由

```typescript
// app/(dashboard)/dashboard/page.tsx
import { getServerSession } from "next-auth";

export default async function DashboardPage() {
  const session = await getServerSession();

  if (!session) {
    redirect("/login");
  }

  // 获取用户数据
  const budgets = await prisma.budget.findMany({
    where: { userId: session.user.id },
  });

  return <Dashboard budgets={budgets} />;
}
```

#### 支持多种登录方式

```typescript
providers: [
  // 邮箱密码
  CredentialsProvider({...}),

  // Google OAuth（未来）
  GoogleProvider({
    clientId: process.env.GOOGLE_CLIENT_ID,
    clientSecret: process.env.GOOGLE_CLIENT_SECRET,
  }),

  // GitHub OAuth（未来）
  GitHubProvider({
    clientId: process.env.GITHUB_ID,
    clientSecret: process.env.GITHUB_SECRET,
  })
]

优势：
✓ MVP 用邮箱密码
✓ 未来一键添加 OAuth
✓ 无需重写认证逻辑
```

---

## 项目结构

```
savvly/
├─ app/                          # Next.js 14 App Router
│  │
│  ├─ (auth)/                    # 认证相关页面（不需要登录）
│  │  ├─ login/
│  │  │  └─ page.tsx             # 登录页
│  │  ├─ register/
│  │  │  └─ page.tsx             # 注册页
│  │  ├─ verify-email/
│  │  │  └─ page.tsx             # 邮箱验证（学生免费）
│  │  └─ layout.tsx              # 认证布局（居中卡片）
│  │
│  ├─ (dashboard)/               # 主应用（需要登录）
│  │  ├─ dashboard/
│  │  │  └─ page.tsx             # 仪表板
│  │  ├─ transactions/
│  │  │  ├─ page.tsx             # 交易列表
│  │  │  └─ [id]/page.tsx        # 交易详情
│  │  ├─ budgets/
│  │  │  ├─ page.tsx             # 预算管理
│  │  │  └─ [id]/page.tsx        # 预算详情
│  │  ├─ goals/
│  │  │  └─ page.tsx             # 目标追踪
│  │  ├─ reports/
│  │  │  └─ page.tsx             # 报告分析
│  │  ├─ settings/
│  │  │  ├─ page.tsx             # 设置首页
│  │  │  ├─ profile/page.tsx     # 个人资料
│  │  │  ├─ preferences/page.tsx # 偏好设置
│  │  │  └─ tax/page.tsx         # 税务设置
│  │  ├─ layout.tsx              # 应用布局（侧边栏、顶栏）
│  │  └─ loading.tsx             # 加载状态
│  │
│  ├─ api/                       # API 路由
│  │  ├─ auth/
│  │  │  └─ [...nextauth]/
│  │  │     └─ route.ts          # NextAuth 配置
│  │  ├─ transactions/
│  │  │  ├─ route.ts             # GET (list), POST (create)
│  │  │  └─ [id]/route.ts        # GET, PATCH, DELETE
│  │  ├─ budgets/
│  │  │  ├─ route.ts             # GET (list), POST (create)
│  │  │  ├─ [id]/route.ts        # GET, PATCH, DELETE
│  │  │  └─ rollover/route.ts    # POST (月末回滚)
│  │  ├─ goals/
│  │  │  ├─ route.ts
│  │  │  └─ [id]/route.ts
│  │  ├─ classify/
│  │  │  └─ route.ts             # POST (AI 分类)
│  │  ├─ analytics/
│  │  │  ├─ runway/route.ts      # GET (储蓄跑道)
│  │  │  ├─ spending/route.ts    # GET (支出分析)
│  │  │  └─ income/route.ts      # GET (收入分析)
│  │  └─ webhooks/
│  │     └─ plaid/route.ts       # POST (Plaid webhook，未来)
│  │
│  ├─ layout.tsx                 # 根布局
│  ├─ page.tsx                   # 首页/落地页
│  ├─ loading.tsx                # 全局加载
│  └─ error.tsx                  # 全局错误
│
├─ components/                   # 可复用组件
│  ├─ ui/                        # Shadcn/ui 组件
│  │  ├─ button.tsx
│  │  ├─ card.tsx
│  │  ├─ dialog.tsx
│  │  ├─ input.tsx
│  │  └─ ...
│  │
│  ├─ layout/                    # 布局组件
│  │  ├─ sidebar.tsx             # 侧边栏
│  │  ├─ topbar.tsx              # 顶部栏
│  │  └─ footer.tsx
│  │
│  ├─ dashboard/                 # 仪表板组件
│  │  ├─ available-income.tsx    # 可用收入卡片
│  │  ├─ runway-widget.tsx       # 储蓄跑道小部件
│  │  ├─ budget-summary.tsx      # 预算摘要
│  │  └─ recent-transactions.tsx # 最近交易
│  │
│  ├─ budget/                    # 预算相关组件
│  │  ├─ budget-card.tsx         # 预算卡片
│  │  ├─ budget-form.tsx         # 预算表单
│  │  ├─ rollover-config.tsx     # 回滚配置
│  │  └─ category-icon.tsx       # 类别图标选择器
│  │
│  ├─ transaction/               # 交易相关组件
│  │  ├─ transaction-list.tsx    # 交易列表
│  │  ├─ transaction-form.tsx    # 交易表单
│  │  ├─ transaction-row.tsx     # 交易行
│  │  └─ category-badge.tsx      # 类别标签
│  │
│  ├─ goal/                      # 目标相关组件
│  │  ├─ goal-card.tsx
│  │  ├─ goal-progress.tsx
│  │  └─ milestone-celebration.tsx
│  │
│  └─ charts/                    # 图表组件
│     ├─ income-chart.tsx        # 收入趋势图
│     ├─ spending-chart.tsx      # 支出饼图
│     └─ runway-chart.tsx        # 跑道趋势图
│
├─ lib/                          # 工具函数
│  ├─ prisma.ts                  # Prisma 客户端单例
│  ├─ auth.ts                    # 认证工具函数
│  ├─ classifier.ts              # 交易分类器
│  ├─ rollover.ts                # 预算回滚逻辑
│  ├─ runway.ts                  # 储蓄跑道计算
│  ├─ tax.ts                     # 税务计算
│  ├─ utils.ts                   # 通用工具（日期、格式化等）
│  └─ constants.ts               # 常量定义
│
├─ types/                        # 类型定义
│  ├─ index.ts                   # 导出所有类型
│  ├─ budget.ts
│  ├─ transaction.ts
│  └─ goal.ts
│
├─ hooks/                        # 自定义 React Hooks
│  ├─ use-budgets.ts
│  ├─ use-transactions.ts
│  └─ use-runway.ts
│
├─ prisma/
│  ├─ schema.prisma              # 数据库模型
│  ├─ seed.ts                    # 种子数据（开发用）
│  └─ migrations/                # 数据库迁移
│
├─ public/                       # 静态资源
│  ├─ images/
│  └─ icons/
│
├─ .env.local                    # 环境变量（不提交 Git）
├─ .env.example                  # 环境变量模板
├─ next.config.js                # Next.js 配置
├─ tailwind.config.js            # Tailwind 配置
├─ tsconfig.json                 # TypeScript 配置
├─ package.json
├─ .eslintrc.json                # ESLint 配置
├─ .prettierrc                   # Prettier 配置
└─ README.md
```

---

## 数据库设计（Prisma Schema）

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ============================================================
// 用户模型
// ============================================================

model User {
  id            String    @id @default(cuid())
  email         String    @unique
  emailVerified DateTime?
  password      String    // bcrypt hashed
  name          String?
  avatar        String?
  isStudent     Boolean   @default(false)

  // 关系
  settings      UserSettings?
  accounts      Account[]
  sessions      Session[]
  budgets       Budget[]
  transactions  Transaction[]
  goals         Goal[]

  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}

// 用户设置
model UserSettings {
  id                String  @id @default(cuid())
  userId            String  @unique
  user              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  // 地区和税务
  country           String  @default("CA") // CA, US
  province          String? // BC, ON, QC...
  state             String? // CA, NY, TX...
  taxRate           Float   @default(0.28) // 28%

  // 预算偏好
  budgetRollover    Boolean @default(true)
  defaultCurrency   String  @default("CAD")

  // 模式选择
  mode              String  @default("accumulation") // "accumulation" | "runway"

  // 跑道模式设置
  runwayReserved    Float   @default(0) // 预留金额（大额支出）
  runwayTarget      DateTime? // 目标日期（如毕业）

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
}

// ============================================================
// 预算模型
// ============================================================

model Budget {
  id              String        @id @default(cuid())
  userId          String
  user            User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  // 基本信息
  category        String        // "食品", "交通", "房租"
  icon            String?       // emoji: "🍔", "🚗", "🏠"
  color           String?       // hex color
  amount          Float         // 月度预算金额

  // 回滚策略
  rolloverType    String        @default("full") // "full" | "partial" | "none" | "goal"
  rolloverAmount  Float         @default(0) // 上月结转金额

  // 特殊标记
  isTaxReserve    Boolean       @default(false) // 是否税务预留
  isFixed         Boolean       @default(false) // 是否固定支出（房租）

  // 关系
  transactions    Transaction[]

  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt

  @@unique([userId, category])
  @@index([userId])
}

// ============================================================
// 交易模型
// ============================================================

model Transaction {
  id              String    @id @default(cuid())
  userId          String
  user            User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  // 基本信息
  date            DateTime  @default(now())
  amount          Float
  description     String
  note            String?   // 用户备注

  // 分类
  budgetId        String?
  budget          Budget?   @relation(fields: [budgetId], references: [id])
  category        String?   // 冗余存储，方便查询

  // AI 分类
  isClassified    Boolean   @default(false)
  classifiedBy    String?   // "ai" | "user" | "rule"
  confidence      Float?    // AI 置信度 0-1

  // 可抵税
  isDeductible    Boolean   @default(false)
  deductionNote   String?   // 抵税说明

  // 类型
  type            String    @default("expense") // "income" | "expense" | "transfer"

  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt

  @@index([userId, date])
  @@index([userId, budgetId])
  @@index([type])
}

// ============================================================
// 目标模型
// ============================================================

model Goal {
  id              String    @id @default(cuid())
  userId          String
  user            User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  // 基本信息
  name            String    // "应急基金", "旅行基金"
  description     String?
  icon            String?   // emoji
  color           String?

  // 金额
  targetAmount    Float
  currentAmount   Float     @default(0)

  // 跑道模式专属
  isRunwayGoal    Boolean   @default(false) // 大额支出倒计时
  deadline        DateTime? // 截止日期

  // 状态
  isCompleted     Boolean   @default(false)
  completedAt     DateTime?

  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt

  @@index([userId])
}

// ============================================================
// NextAuth 必需模型
// ============================================================

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}
```

---

## AI 分类实现方案

### Phase 1：规则引擎（MVP）

```typescript
// lib/classifier.ts

interface ClassificationRule {
  keywords: string[];
  category: string;
  confidence: number;
}

const rules: ClassificationRule[] = [
  // 食品
  {
    keywords: ['超市', 'grocery', 'safeway', 'save-on-foods', 'walmart', 'costco'],
    category: '食品',
    confidence: 0.9
  },
  {
    keywords: ['餐厅', 'restaurant', 'cafe', 'coffee', 'starbucks', 'tim hortons'],
    category: '餐饮',
    confidence: 0.85
  },

  // 交通
  {
    keywords: ['skytrain', 'translink', 'compass', 'uber', 'lyft', 'taxi'],
    category: '交通',
    confidence: 0.9
  },
  {
    keywords: ['gas', '加油', 'petro-canada', 'shell', 'esso'],
    category: '交通',
    confidence: 0.85
  },

  // 住房
  {
    keywords: ['rent', '房租', 'landlord'],
    category: '房租',
    confidence: 0.95
  },
  {
    keywords: ['hydro', 'bc hydro', 'fortis', 'electricity'],
    category: '公用事业',
    confidence: 0.9
  },

  // 娱乐
  {
    keywords: ['netflix', 'spotify', 'disney+', 'youtube premium'],
    category: '订阅',
    confidence: 0.95
  },
  {
    keywords: ['cineplex', 'movie', 'concert', 'ticket'],
    category: '娱乐',
    confidence: 0.8
  },

  // 学习
  {
    keywords: ['tuition', '学费', 'textbook', 'course'],
    category: '教育',
    confidence: 0.95
  },

  // 其他
  // ... 更多规则
];

export function classifyByRules(description: string): {
  category: string;
  confidence: number;
} {
  const lowerDesc = description.toLowerCase();

  // 寻找匹配的规则
  for (const rule of rules) {
    for (const keyword of rule.keywords) {
      if (lowerDesc.includes(keyword.toLowerCase())) {
        return {
          category: rule.category,
          confidence: rule.confidence
        };
      }
    }
  }

  // 无匹配，返回默认
  return {
    category: '其他',
    confidence: 0.3
  };
}

优势：
✓ 免费
✓ 即时响应
✓ 可控（添加规则容易）
✓ 适合 MVP 验证

劣势：
⚠️ 覆盖不全面
⚠️ 无法学习
⚠️ 需要持续维护规则
```

### Phase 2：OpenAI API（升级）

```typescript
// lib/classifier.ts

import OpenAI from 'openai';

const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

const categories = [
  '食品', '餐饮', '交通', '房租', '公用事业',
  '订阅', '娱乐', '教育', '医疗', '其他'
];

export async function classifyByAI(description: string): Promise<{
  category: string;
  confidence: number;
}> {
  try {
    const response = await openai.chat.completions.create({
      model: 'gpt-4-turbo',
      messages: [
        {
          role: 'system',
          content: `你是一个交易分类器。给定交易描述，返回最合适的类别。可用类别：${categories.join(', ')}`
        },
        {
          role: 'user',
          content: `分类这笔交易："${description}"`
        }
      ],
      functions: [
        {
          name: 'classify_transaction',
          description: '将交易分类到预定义类别',
          parameters: {
            type: 'object',
            properties: {
              category: {
                type: 'string',
                enum: categories,
                description: '最合适的类别'
              },
              confidence: {
                type: 'number',
                minimum: 0,
                maximum: 1,
                description: '分类置信度'
              },
              reasoning: {
                type: 'string',
                description: '分类理由'
              }
            },
            required: ['category', 'confidence']
          }
        }
      ],
      function_call: { name: 'classify_transaction' }
    });

    const result = JSON.parse(
      response.choices[0].message.function_call?.arguments || '{}'
    );

    return {
      category: result.category,
      confidence: result.confidence
    };
  } catch (error) {
    console.error('AI classification failed:', error);
    // 回退到规则引擎
    return classifyByRules(description);
  }
}

成本估算：
- GPT-4 Turbo：~$0.01 per 1K tokens
- 平均每次分类：~200 tokens
- 成本：~$0.002 per classification
- 100 笔交易：~$0.20

优势：
✓ 智能理解上下文
✓ 自动学习新模式
✓ 多语言支持
✓ 高准确度

劣势：
⚠️ 成本（但可控）
⚠️ 依赖外部 API
⚠️ 响应时间略慢（~1-2 秒）
```

### 混合策略（最佳）

```typescript
// lib/classifier.ts

export async function classifyTransaction(
  description: string,
  userHistory?: Transaction[]
): Promise<{
  category: string;
  confidence: number;
  method: 'rule' | 'ai' | 'history';
}> {
  // 1. 先尝试用户历史（最精准）
  if (userHistory && userHistory.length > 0) {
    const historyMatch = findHistoricalMatch(description, userHistory);
    if (historyMatch && historyMatch.confidence > 0.8) {
      return {
        category: historyMatch.category,
        confidence: historyMatch.confidence,
        method: 'history'
      };
    }
  }

  // 2. 尝试规则引擎（快速 + 免费）
  const ruleResult = classifyByRules(description);
  if (ruleResult.confidence > 0.7) {
    return {
      ...ruleResult,
      method: 'rule'
    };
  }

  // 3. 最后使用 AI（智能但有成本）
  const aiResult = await classifyByAI(description);
  return {
    ...aiResult,
    method: 'ai'
  };
}

function findHistoricalMatch(
  description: string,
  history: Transaction[]
): { category: string; confidence: number } | null {
  // 寻找相似的历史交易
  const similar = history.find(t =>
    similarity(t.description, description) > 0.8
  );

  if (similar && similar.category) {
    return {
      category: similar.category,
      confidence: 0.9
    };
  }

  return null;
}

优势：
✓ 综合各方法优势
✓ 成本可控（AI 作为兜底）
✓ 速度快（优先本地方法）
✓ 准确度高（多层验证）
```

---

## MVP 开发时间表（12 周）

### Week 1-2: 基础设施搭建

```
任务清单：
□ Next.js 14 项目初始化
  - npx create-next-app@latest savvly --typescript --tailwind --app
  - 配置 ESLint + Prettier
  - 安装 Shadcn/ui

□ Prisma + PostgreSQL 设置
  - 安装 Prisma
  - 编写 schema.prisma
  - 创建初始 Migration
  - 连接 Railway PostgreSQL

□ NextAuth 认证流程
  - 安装 NextAuth v5
  - 配置邮箱密码登录
  - 实现注册/登录页面
  - Session 管理

□ 基础 UI 组件
  - 安装常用 Shadcn 组件
  - 创建布局（侧边栏、顶栏）
  - 响应式设计测试

□ 部署到 Vercel
  - 连接 GitHub
  - 配置环境变量
  - 首次部署成功

交付物：
✓ 可访问的网站（有登录功能）
✓ 数据库连接成功
✓ 基础 UI 框架完成
```

### Week 3-4: 核心功能（交易和预算）

```
任务清单：
□ 交易管理
  - 手动添加交易 API
  - 交易列表页面
  - 交易详情/编辑
  - 交易删除

□ 预算管理
  - 创建预算 API
  - 预算列表页面
  - 预算编辑
  - 类别图标选择器

□ 预算回滚逻辑
  - 实现 4 种回滚策略
  - 月末自动回滚
  - 回滚配置 UI

□ 基础仪表板
  - 可用收入卡片
  - 预算摘要
  - 最近交易列表

交付物：
✓ 可以添加/管理交易
✓ 可以设置/调整预算
✓ 预算回滚功能工作
✓ 基础仪表板可视化
```

### Week 5-6: 不规则收入特性

```
任务清单：
□ 税务自动预留
  - 地区/税率配置 UI
  - 收入自动分配逻辑
  - 税务预留账户
  - 年度税务报告

□ 收入可视化
  - 收入趋势图（Recharts）
  - 收入平滑算法
  - 高/低/平均收入显示

□ 最低月预算计算
  - 基于历史数据计算
  - "安全基线"建议
  - 超额收入分配规则

□ 目标追踪
  - 创建目标（应急基金等）
  - 目标进度条
  - 里程碑庆祝动画

交付物：
✓ 税务预留自动化
✓ 收入趋势可视化
✓ 最低月预算功能
✓ 目标追踪完整流程
```

### Week 7-8: AI 和优化

```
任务清单：
□ 交易分类
  - 规则引擎实现
  - 分类 API
  - 一键修正 UI
  - 分类历史学习

□ 数据可视化
  - 支出饼图
  - 月度对比
  - 分类趋势

□ 性能优化
  - Server Components 优化
  - 图片优化
  - 数据库查询优化
  - Loading 状态完善

□ 错误处理
  - Error Boundary
  - Toast 通知
  - 表单验证

交付物：
✓ AI 分类工作良好
✓ 丰富的数据可视化
✓ 页面加载快速
✓ 错误处理完善
```

### Week 9-10: 跑道模式增强（Phase 1.5）

```
任务清单：
□ 储蓄跑道小部件
  - 跑道计算算法
  - 跑道仪表盘卡片
  - 趋势图

□ 大额支出倒计时
  - 跑道目标创建
  - 倒计时显示
  - 关联跑道计算

□ 负现金流友好文案
  - 检测负现金流
  - 调整提示语
  - 积极框架重写

□ 测试和 bug 修复
  - 端到端测试
  - 边缘情况处理
  - 跨浏览器测试

交付物：
✓ 储蓄跑道功能完整
✓ 创始人可以用自己的产品
✓ 主要 bug 已修复
```

### Week 11-12: 完善和演示准备

```
任务清单：
□ UI/UX 打磨
  - 动画效果
  - 交互细节
  - 移动端优化
  - 暗色模式（可选）

□ 文档编写
  - 技术报告（学期作业）
  - README.md
  - API 文档
  - 用户手册

□ 演示准备
  - 演示视频录制
  - PPT 制作
  - Demo 数据准备
  - 演讲稿

□ 最终测试
  - 功能完整性检查
  - 性能测试
  - 安全检查
  - 备份数据库

交付物：
✓ 抛光的产品
✓ 完整的技术文档
✓ 演示材料齐全
✓ 准备好展示
```

---

## 部署方案

### 开发环境

```bash
# 本地开发
npm run dev
# → http://localhost:3000

# 数据库
# Railway PostgreSQL (开发数据库)
# → 免费 $5/月额度
```

### 生产环境

```bash
# Vercel 部署（自动）
git push origin main
# → 自动部署到 https://savvly.vercel.app

# 数据库
# Railway PostgreSQL (生产数据库)
# → Starter Plan $5/月

# 环境变量（Vercel Dashboard）
DATABASE_URL=postgresql://...
NEXTAUTH_SECRET=...
NEXTAUTH_URL=https://savvly.vercel.app
OPENAI_API_KEY=sk-... (Phase 2)
```

### 成本估算

```
开发期间（学期）：
├─ Vercel：$0（Hobby Plan 免费）
├─ Railway：$0-5（免费额度通常够用）
├─ OpenAI：$0（Phase 1 用规则引擎）
└─ 域名：$0（使用 .vercel.app）

总计：~$0-5/月

生产运营（未来）：
├─ Vercel：$20/月（Pro Plan）
├─ Railway：$5-20/月（根据流量）
├─ OpenAI：$10-50/月（根据用户量）
└─ 域名：$10-15/年

总计：~$35-90/月（100-1000 用户）
```

---

## 备选方案对比

### 为什么不选方案 B（Vue.js）？

```
方案 B 优势：
✓ 上手更快
✓ 文档更友好
✓ 适合快速原型

但劣势明显：
✗ React 生态更大（求职优势）
✗ Next.js 是行业标准
✗ 移动端扩展困难（Vue ≠ React Native）
✗ 简历价值低
✗ 社区资源少

结论：
短期节省 1-2 周开发时间
长期损失技能积累和扩展性
→ 不值得
```

### 为什么不选方案 C（NestJS 微服务）？

```
方案 C 优势：
✓ 展示技术深度
✓ 简历杀手级
✓ 企业级架构

但劣势明显：
✗ 开发时间长（可能超出学期）
✗ 过度工程（MVP 不需要微服务）
✗ 维护复杂
✗ 部署成本高

结论：
适合大型团队项目，不适合 MVP
等产品验证成功再重构
```

---

## 风险与缓解策略

### 风险 1：学期时间不够

**缓解策略**：

```
- 严格按时间表执行
- 使用 Shadcn/ui 加速 UI 开发
- Phase 1.5（跑道模式）可延后
- 优先核心功能，砍掉非必需功能
- 每周复盘，及时调整
```

### 风险 2：技术学习曲线

**缓解策略**：

```
- Week 1 专注学习 Next.js 14
- 跟随官方教程（Next.js Learn）
- 遇到问题优先查官方文档
- 利用 ChatGPT/Claude 辅助学习
- 参考开源项目（shadcn/taxonomy 等）
```

### 风险 3：数据库设计变更

**缓解策略**：

```
- Prisma Migration 管理变更
- 早期频繁迭代没问题
- 保持 schema 简单清晰
- 重要变更前先测试
- 定期备份数据库
```

### 风险 4：部署问题

**缓解策略**：

```
- 早期（Week 2）就部署到 Vercel
- 持续部署，及早发现问题
- 使用 Vercel Preview Deployments
- 环境变量提前配置好
- 保持 .env.example 更新
```

---

## 成功标准

### 学期作业维度

```
✓ 功能完整性（30%）
  - 核心功能全部实现
  - 无严重 bug
  - 用户流程完整

✓ 技术深度（30%）
  - 现代技术栈
  - 代码质量高
  - 架构设计合理

✓ 文档完善（20%）
  - 技术报告详细
  - 代码注释清晰
  - README 完整

✓ 演示效果（20%）
  - 界面美观
  - 交互流畅
  - 讲解清晰
```

### 产品维度（未来）

```
✓ 用户可用性
  - 创始人自己日常使用
  - 5-10 个早期用户测试
  - 正面反馈 > 80%

✓ 技术稳定性
  - 无数据丢失
  - 99%+ 可用性
  - 性能良好（<2 秒加载）

✓ 扩展性
  - 代码易于维护
  - 功能易于添加
  - 数据库设计合理
```

---

## 下一步行动

### 本周（立即）

```
1. ✅ 技术选型确定（已完成）
2. ⏭️ UI/UX 风格确定（进行中）
3. ⏭️ 创建 GitHub 仓库
4. ⏭️ 初始化 Next.js 项目
5. ⏭️ 配置开发环境
```

### 下周（Week 1）

```
1. 完成 Prisma Schema 设计
2. 配置 NextAuth
3. 实现基础登录/注册
4. 首次部署到 Vercel
5. 开始 Week 1-2 任务
```

---

## 参考资源

### 官方文档

- Next.js: https://nextjs.org/docs
- Prisma: https://www.prisma.io/docs
- Tailwind CSS: https://tailwindcss.com/docs
- Shadcn/ui: https://ui.shadcn.com
- NextAuth: https://next-auth.js.org

### 学习资源

- Next.js Learn: https://nextjs.org/learn
- Prisma Getting Started: https://www.prisma.io/docs/getting-started
- Tailwind UI: https://tailwindui.com (付费但值得)

### 开源参考

- Taxonomy (shadcn): https://github.com/shadcn/taxonomy
- Next.js Examples: https://github.com/vercel/next.js/tree/canary/examples
- Prisma Examples: https://github.com/prisma/prisma-examples

### 社区

- Next.js Discord: https://nextjs.org/discord
- Prisma Discord: https://pris.ly/discord
- r/nextjs: https://reddit.com/r/nextjs

---

**文档版本**：v1.0
**创建日期**：2025 年
**最后更新**：技术选型最终确定
**状态**：✅ 已确认，开始执行

**下一步**：生成 UI/UX 风格指南文档
